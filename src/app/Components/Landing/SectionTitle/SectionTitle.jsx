const SectionTitle = ({title}) => {
    return (
        <h1 className="text-2xl md:text-4xl max-w-[600px] mx-auto font-bold text-center pb-6">{title}</h1>
    );
};

export default SectionTitle;